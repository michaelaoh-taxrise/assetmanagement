# TaxRise Asset Management

A lightweight browser-based asset homebase for tracking company equipment assigned to employees and assets pending return after termination.

Open `index.html` directly in a browser, or serve the folder with any static file server.
